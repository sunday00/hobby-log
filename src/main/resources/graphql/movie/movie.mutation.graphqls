extend type Mutation {
    logMovie(movieInput: MovieInput): Result
    updateMovie(movieInput: MovieInput): Result
}

input MovieInput {
    id: String,
    movieId: Long!,
    content: String!,
    ratings: Int!,
    logAtStr: String
}

# todo
# log movie with logAtString
